import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Gym = () => {
    return (
        <View>
            <Text>Gym</Text>
        </View>
    );
};

export default Gym;

const styles = StyleSheet.create({});
